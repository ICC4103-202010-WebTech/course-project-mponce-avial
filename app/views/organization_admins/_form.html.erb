<%= form_with(model: organization_admin, local: true) do |form| %>
  <% if organization_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization_admin.errors.count, "error") %> prohibited this organization_admin from being saved:</h2>

      <ul>
        <% organization_admin.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
