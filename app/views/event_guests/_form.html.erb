<%= form_with(model: event_guest, local: true) do |form| %>
  <% if event_guest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_guest.errors.count, "error") %> prohibited this event_guest from being saved:</h2>

      <ul>
        <% event_guest.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
