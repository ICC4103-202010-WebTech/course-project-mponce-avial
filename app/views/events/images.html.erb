<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <h1>Images</h1>
  <hr>
</head>

<% if @event.imgEvent.attached? %>
  <%@event.imgEvent.each do |img| %>
    <div>
      <%= image_tag(img,width: "15%")%>
      <h4><%=img.filename%></h4>
    </div>
  <% end %>
<% else %>
<% end %>