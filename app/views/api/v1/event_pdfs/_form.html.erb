<%= form_with(model: event_pdf, local: true) do |form| %>
  <% if event_pdf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_pdf.errors.count, "error") %> prohibited this event_pdf from being saved:</h2>

      <ul>
        <% event_pdf.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
